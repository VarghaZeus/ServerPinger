# ServerPinger runtime dependencies.
# Pure-Python only: installs on 32-bit ARM Raspberry Pi, Windows Server and x86 Linux
# without a C toolchain. (MarkupSafe ships optional C speedups but falls back to a
# pure-Python implementation when no compiler is present.)
Flask==3.0.3
Werkzeug==3.0.3
Jinja2==3.1.4
MarkupSafe==2.1.5
itsdangerous==2.2.0
click==8.1.7
blinker==1.8.2
waitress==3.0.0
